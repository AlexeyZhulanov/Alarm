# Alarm App (Android, Kotlin)

> Надёжный пользовательский будильник для Android с поддержкой повторов (snooze), собственным экраном срабатывания и устойчивой архитектурой запуска.

---

## 📸 Скриншоты
<img width="200" height="432" alt="image" src="https://github.com/user-attachments/assets/7a008b93-b99a-4d70-bf0d-440ba3a6bc23" />
<img width="200" height="432" alt="image" src="https://github.com/user-attachments/assets/52c33e31-8c74-4c1e-9348-c6e916470d77" />
<img width="200" height="432" alt="image" src="https://github.com/user-attachments/assets/2c642ca0-8869-4080-9405-79acd29bab8a" />
<img width="200" height="432" alt="image" src="https://github.com/user-attachments/assets/d62af9f5-f669-4ad9-ba80-829132f9348e" />

---

## 📱 О проекте

Это пользовательское Android-приложение «Будильник», реализованное на Kotlin с упором на максимально стабильный запуск будильника на современных версиях Android (включая Android 14+).

Проект разрабатывался как полноценное production-решение, с учётом ограничений фонового выполнения, блокировок запуска Activity из background и особенностей современных версий Android.

Основная цель проекта — корректная и устойчивая работа будильника без использования системных привилегий.

---

## 🚀 Основные возможности

- список всех будильников (RecyclerView)
- создание и редактирование будильников
- собственный экран срабатывания будильника
- повтор (snooze) с настраиваемым:
  - количеством повторов
  - интервалом между повторами
- выбор мелодии из встроенного набора
- вибрация
- выбор обоев приложения
- выбор цветовой темы приложения
- управление будильником с экрана блокировки
- поддержка аппаратных кнопок (громкости) для перевода будильника в режим повтора
- отдельный экран настроек

---

## 🧱 Технологический стек

- Kotlin
- MVVM архитектура
- Room (локальная база данных)
- Hilt (dependency injection)
- Coroutines
- RecyclerView
- Material Components
- сторонние UI-библиотеки:
  - пульсирующие анимации
  - кастомный слайдер

---

## 🧠 Архитектура запуска будильника

Для максимально стабильной работы используется следующая цепочка:

AlarmManager.setAlarmClock(...) -> BroadcastReceiver -> Foreground Service -> Notification + Full-screen intent -> Activity (экран будильника)

Фактическое выполнение будильника (звук, вибрация, логика повторов) происходит **только внутри foreground-сервиса**.  
UI не участвует в запуске будильника и используется исключительно как интерфейс.

### Используемые механизмы

- `AlarmManager.setAlarmClock(...)`
- отдельный `BroadcastReceiver` для получения события
- запуск `ForegroundService` для выполнения будильника
- уведомление с категорией `CATEGORY_ALARM`
- `fullScreenIntent` для показа экрана будильника
- воспроизведение звука через `MediaPlayer` с `AudioAttributes.USAGE_ALARM`

Такой подход позволяет:

- не зависеть от возможности запуска Activity из background
- не зависеть от состояния экрана
- не зависеть от жизненного цикла UI

---

## 🔁 Повторы (Snooze)

Повтор реализован на уровне сервиса.

При выборе повтора:

- текущий будильник корректно останавливается
- сервис самостоятельно планирует следующий запуск через `AlarmManager`
- состояние повторов не зависит от Activity или Fragment

Команды «выключить» и «повторить» отправляются в сервис через `Intent`-действия.

---

## 🔊 Работа со звуком

Звук будильника воспроизводится через `MediaPlayer` с использованием:
`AudioAttributes.USAGE_ALARM`


Это позволяет:

- использовать системную громкость будильника
- корректно работать с режимами «Не беспокоить»
- не изменять громкость мультимедиа пользователя

---

## 🔄 Восстановление будильников

Для повышения надёжности реализованы:

- `BOOT_COMPLETED` receiver — пересоздание всех активных будильников после перезагрузки устройства
- восстановление всех активных будильников при каждом запуске приложения

Таким образом, приложение автоматически восстанавливает все запланированные будильники при:

- перезагрузке телефона
- повторном запуске приложения после выгрузки процесса

---

## ⚠️ Почему всё равно не существует 100% гарантии запуска

Даже при использовании:

- `setAlarmClock`
- foreground-сервиса
- `BOOT_COMPLETED` receiver
- восстановления при запуске приложения

стороннее приложение не может гарантировать 100% срабатывание будильника.

Причины находятся на уровне платформы:

- пользователь может выполнить **Force Stop** приложения
- производители устройств (vendor-прошивки) могут агрессивно ограничивать фоновую работу
- системные индикаторы и приоритеты отличаются от системных приложений

После force-stop:

- AlarmManager события не доставляются
- broadcast-receiver не вызывается
- сервисы не запускаются

До следующего ручного запуска приложения пользователем.

По этой причине невозможно обеспечить абсолютную надёжность, сравнимую с системным будильником.

Проект реализует максимально возможный уровень надёжности, доступный для стороннего Android-приложения.

---

## 🏗 Архитектура проекта

Проект построен по MVVM:

- UI (Activity / Fragment)
- ViewModel
- слой данных (Room, DAO, сервисы)
- отдельный слой работы с будильником (AlarmManager wrapper)

Критически важная бизнес-логика (запуск, повтор, остановка будильника) вынесена в сервис и не зависит от UI.

---

## 🧩 Дополнительно

- поддержка разных цветовых тем
- выбор фонового изображения
- встроенный список мелодий
- собственный экран будильника поверх lock-screen
- поддержка управления аппаратными кнопками

---

## 📌 Итог

Проект демонстрирует:

- глубокое понимание современных ограничений Android background execution
- корректное использование foreground-сервисов и alarm-API
- практическую архитектуру надёжного будильника
- работу с Room, Hilt и Coroutines в реальном приложении
- построение пользовательского будильника уровня production
